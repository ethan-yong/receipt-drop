# Copy to supabase/functions/.env for local edge function secrets.
# Do not put SUPABASE_* vars here — the CLI injects those automatically.

# Base URL only — ocr-proxy appends /ocr. Use host.docker.internal so the
# edge runtime container can reach uvicorn on the host (not localhost).
OCR_SERVICE_URL=http://host.docker.internal:8081
OCR_SERVICE_SECRET=your-local-ocr-secret

# GOOGLE_PLACES_API_KEY is required by enrich-transaction and places-proxy
# but deliberately not templated here — set it directly in this local .env
# (never commit it) or via `supabase secrets set` for deployed environments.
# GOOGLE_PLACES_API_KEY=your-google-places-api-key

# LLM merchant-intelligence gateway (enrich-transaction's receipt-understanding
# step) — an OpenAI-compatible LiteLLM/vLLM endpoint, same one probed by
# scripts/inspect_llm_endpoint.py. A LAN IP works from the local edge runtime
# container; localhost would need host.docker.internal like OCR_SERVICE_URL.
VLLM_BASE_URL=http://192.168.2.134:31180/v1
VLLM_API_KEY=sk-your-litellm-virtual-key
VLLM_MODEL_NAME=local/vllm/openai/gpt-oss-20b
VLLM_REASONING_EFFORT=low

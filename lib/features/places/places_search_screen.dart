import 'package:flutter/material.dart';

/// Placeholder — Task 15 / Places flow.
class PlacesSearchScreen extends StatelessWidget {
  const PlacesSearchScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Search place')),
      body: const Center(child: Text('Places search — coming soon.')),
    );
  }
}

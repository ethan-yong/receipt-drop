import 'package:flutter/material.dart';

/// Map of spend by place (placeholder for Task 17).
class SpendMapScreen extends StatelessWidget {
  const SpendMapScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Map')),
      body: const Center(
        child: Text('Spend map will appear here.'),
      ),
    );
  }
}

import 'package:flutter/material.dart';

/// Placeholder — full teaser in Task 18.
class FriendsTeaserScreen extends StatelessWidget {
  const FriendsTeaserScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Friends')),
      body: const Center(child: Text('Friends teaser — coming soon.')),
    );
  }
}
